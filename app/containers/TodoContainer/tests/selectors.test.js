// import { fromJS } from 'immutable';
// import { makeSelectTodoContainerDomain } from '../selectors';

// const selector = makeSelectTodoContainerDomain();

describe('makeSelectTodoContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
